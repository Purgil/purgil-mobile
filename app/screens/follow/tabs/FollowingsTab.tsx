import { Text, View } from '~/components/styled'
import { memo } from 'react'

function FollowingsTab() {
  return (
    <View>
      <Text>Followings</Text>
    </View>
  )
}

export default memo(FollowingsTab)
